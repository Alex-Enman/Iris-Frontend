
  # Iris Marketplace Webapp Design

  This is a code bundle for Iris Marketplace Webapp Design. The original project is available at https://www.figma.com/design/6AprLC2TnnqUIVelb95wZb/Iris-Marketplace-Webapp-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  